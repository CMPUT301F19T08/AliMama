<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alimama">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".page.FriendPage"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT"></action>

            </intent-filter>
        </activity>



        <activity
            android:name=".GoogleMaps"
            android:label="@string/title_activity_google_maps"></activity>
        <activity
            android:name=".MoodMap"
            android:label="@string/title_activity_mood_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MoodHistory"
            android:label="@string/title_activity_mood_history"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".Mainmenu" />
        <activity android:name=".ParticipantLoginSignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>